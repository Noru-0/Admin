version: 2.1

executors:
  node-executor:
    docker:
      - image: cimg/node:20.1.0

jobs:
  build:
    executor: node-executor
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Tests
          command: npm test

workflows:
  build_and_test:
    jobs:
      - build
